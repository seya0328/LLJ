<div class="cover-show">
  <div class="container">
    <div class="row justify-content-center">
      <h1 class="font-weight-normal mt-5 mb-5">会員詳細</h1>
    </div>
    <div class="row justify-content-center d-block">

      <h5>会員ID</h5>
        <p><%= @user.id %></p>
      <h5>氏名</h5>
        <p><%= @user.full_name %></p>

      <h5>メールアドレス</h5>
        <p><%= @user.email %></p>
      <h5>会員ステータス</h5>
        <td><% if @user.is_deleted == false %>
　           有効
　           <% else %> 
　           退会
　           <% end %></td>

      <%#= link_to "編集する", edit_admin_customer_path(@customer.id) %>
      <%= link_to "コメント履歴一覧を見る", admin_user_post_comments_path(@user.id) %>

    </div>
    </div>
  </div>
</div>